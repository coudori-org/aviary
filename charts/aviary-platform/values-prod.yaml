# EKS overrides. No docker-compose proxies in prod.

externalServices:
  enabled: false

imageWarmer:
  enabled: true

sharedWorkspace:
  storageClassName: efs-sc
  # hostPath deliberately empty — EFS StorageClass provisions the PV
  # dynamically, no static PV needed.
