[project]
name = "aviary-inference-router"
version = "0.1.0"
description = "Aviary Inference Router — single endpoint for all LLM backends"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic",
]
