[project]
name = "aviary-credential-proxy"
version = "0.1.0"
description = "Aviary Credential Proxy — injects secrets from Vault into outbound requests"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "asyncpg",
    "pydantic",
]
